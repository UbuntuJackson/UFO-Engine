#include <vector>
#include <map>

/*typedef int State;
const State STATE_ACCESS_PERMSSION = -1;

class StateMachine{
    std::map<State, State>
    std::map<State, bool> on_and_off

    void SetState(State _state){
        for(const auto& : )
    }
};*/